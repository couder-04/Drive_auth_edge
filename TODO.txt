# DriveAuth Edge — TODO (from roadmap-2026-07.md)

## ★ DO NOW — Own face (top priority)
- [x] **Replace RDJ face enroll/genuine with YOUR own face**
      → enroll + Stage 2 PAD/calibrators trained; re-check thresholds before
        applying `phase2b_suggested.env` (face overlap improved via PAD reject)

## Deferred (as planned — not blocking Mac work)
- [ ] **Real CAN → re-run** `scripts/train_behavioral_bakeoff.py` when you have
      recorder dumps under `data/*/behavioral/{genuine,attack}/`
      (8 features in `BEHAVIORAL_FEATURE_KEYS`); re-enroll after. Synth bake-off
      metrics are pipeline-only, not production FAR/FRR.
- [ ] **Do not** apply `phases/phase2b_suggested.env` as default until you
      re-check policy bars (post–Stage 2 PAD / calibrators).
- [ ] Wire GPS into Nova (`update_vehicle_context`) when integrating pipeline
      (manual GPS/zone/speed in dashboard until then)
- [ ] Replace synth finger / CAN with real HW captures when sensors arrive
      (OOD voice+face Stage-1 set already real/TTS; finger OOD still synth)

## Sprint 1 — done
- [x] Voice/face capture protocols + Phase 3 min sets
- [x] Phase 2a voice+face enroll (ECAPA + MobileFaceNet)
- [x] Manual score API + dashboard Nova I/O contract
- [x] Timing side-channel test (`tests/test_security_sprint1.py`)
- [x] OOD-drift attack simulation (`tests/test_security_sprint1.py`)
- [x] README + demo GIF polish
      (`docs/demo.gif` + `scripts/capture_dashboard_demo_gif.py`)

## Phase 1 — Edge (Thor)
- [x] Thor install + deps + GPU/NPU (CPU EP for mock; GPU visible via nvidia-smi)
- [x] End-to-end demo on device (pytest · driveauth-demo ACCEPT · dashboard)
- [x] Baseline hardware profile (CPU / GPU / RAM / latency)
      → `phases/mac.txt` · `phases/thor.txt` · [`phases/phase1.md`](phases/phase1.md)
      (mock auth p95 ≤ 10 ms: Mac 0.8 · Thor 0.9)

## Phase 2a — Off-the-shelf models
- [x] Risk — LightGBM → `risk_gbt.onnx`
- [x] Voice — ECAPA-TDNN pretrained + enrolled
- [x] Face — MobileFaceNet pretrained + enrolled (own-face; RDJ retired)
- [x] Mac latency profile — `phases/phase2a-mac.txt` (`scripts/phase2a_bench.py`)
- [x] Thor latency profile — `phases/phase2a-thor.txt`
      (ECAPA+face CUDA · ORT SM110 build · micro p95 7.7 · high 9.2 ms)
- [ ] Fingerprint — vendor SDK (mock + ManualScores until HW) ## NEED HARDWARE
- [x] Behavioral — LSTM/GRU/GBM bake-off on synth → best wired
      (`scripts/train_behavioral_bakeoff.py` → `behavioral_model.onnx` +
      `behavioral/{driver}.enc` + `behavioral_bakeoff.json`)
      ## Real CAN still required before trusting metrics (see Deferred)
- [x] Trust fusion — Stage 1 static; Stage 2 logreg wired (`trust_fusion.onnx`)
- [x] Real OOD where possible (Stage 1)
      → voice TTS OOD in `data/driver1/ood/voice/` (5 WAVs)
      → face other-id stills in `data/driver1/ood/face/` (20 JPGs)
      → eval: `scripts/eval_ood_negatives.py` → `phases/phase2a_ood_eval.json`
      → tests: `tests/test_ood_stage1.py`
      → finger OOD remains synth until sensor HW

## Stage 1 — Finish Phase 2a (HW-gated remainder)
- [x] Real OOD where possible + static trust fusion locked
- [ ] Finger SDK + real captures → drop `ManualScores` for finger ## NEED HARDWARE
- [ ] Real CAN windows → re-run `train_behavioral_bakeoff.py` ## NEED HARDWARE

## Stage 2 / Phase 2b — Fine-tune (score adapters + PAD; frozen 2a backbones)
- [x] Voice calibrator — `voice_calibrator.onnx` (`scripts/train_voice_calibrator.py`)
- [x] Face PAD + calibrator — `face_pad.onnx` + `face_calibrator.onnx`
      (`scripts/train_face_pad.py`, `train_face_calibrator.py`)
- [x] Behavioral re-bake on **current** CAN (synth stand-in) — refresh
      `behavioral_model.onnx` + `behavioral_bakeoff.json` (retrain when real CAN)
- [x] Trust fusion logistic regression → `trust_fusion.onnx`
- [x] Eval: `phases/phase2a_bio_baseline.json` → `phases/phase2b_bio_eval.json`
      (`scripts/eval_bio_far_frr.py`); overfit audit `scripts/overfit_audit_stage2.py`

## Phase 3 — Datasets
- [x] Txns 50k · voice · face (own-face) · finger/beh/ood synth
- [x] Own-face enroll/genuine/attacks (HW finger/CAN still deferred)
- [ ] Real CAN windows replacing synth behavioral (same 8-feature schema)

## Phase 4 — Training (risk ✅ done)
- [x] Trust fusion logreg → ONNX (`trust_fusion.onnx`)
- [x] Behavioral → ONNX (synth bake-off winner; redo on real CAN)
- [x] Stage-2 heads from Phase 2b (calibrators + PAD; not full ECAPA/MFN FT)

## Phase 5 — Testing
- [x] Timing side-channel test
- [x] OOD-drift simulation
- [x] Real-model failure modes
- [x] Threshold re-baselining after own-face / real models
- [x] Grow toward 150+ tests
      → **167** tests (pytest); see `phases/phase5.md`

## Phase 6 — Benchmarking
- [x] FAR / FRR / EER / ROC · PAD · risk · latency · vs OTP/static MFA/staged
      → `scripts/phase6_benchmark.py` → `phases/phase6_sprint6.json` +
        [`phases/phase6.md`](phases/phase6.md) (Sprint 6 table + ablations)
      → tests: `tests/test_phase6_benchmark.py`

## Phase 7 — Docs
- [x] README refresh (July 2026 status / Phase 2a–3 / Nova contract)
- [x] Demo GIF (`docs/demo.gif` — dashboard ACCEPT → STEP_UP)
- [x] Security assumptions (`docs/security-assumptions.md` · README linked)
- [x] LinkedIn / Medium posts — drafts in [`docs/public-posts.md`](docs/public-posts.md)
      (publish URLs still open; tick table in that file after posting)

## Standalone product (OpenRouter + Railway)
- [x] secrets.env.example · OpenRouter STT/TTS/LLM · intent slot-fill
- [x] Robust `INTENT_SYSTEM_PROMPT` · `ask_field` column TTS follow-ups
- [x] Live `/api/standalone/auth` (ECAPA/face; finger manual) · unified store
- [x] Register Maps home pin · pay GPS → `dist_from_home_km`
- [x] Separate pages: `/manual` · `/standalone` · `/register`
      (Actions / Live pipeline / Result / Audit on both pipeline pages)
- [x] Register **Registered drivers** list (`/api/register/drivers`)
- [x] Dockerfile · railway.toml · [`docs/standalone.md`](docs/standalone.md)
- [x] Local `secrets.env` filled (OpenRouter + Maps) — **do not commit**
- [ ] Deploy Railway volume at `/data` (always-on; Mac can sleep)

## Phase 8 — Publications
- [ ] IV/ITSC or CCS/NDSS · demo video

## Explicit non-goals
- Retrain risk on real txns before ~5k labels
- Apply `phase2b_suggested.env` until face overlap improves
- Latency optimisation before Phase 2a Thor (real model) profile
- ~~Paper before Sprint 6~~ — Sprint 6 table filled; paper unblocked
- Treat synth-CAN behavioral metrics as production biometric quality
