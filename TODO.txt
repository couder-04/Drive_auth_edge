# DriveAuth Edge — Active TODO

Last updated: 2026-08-14

---

## ✅ Done in repo (no action needed)

Code, docs, and scripts below are **landed**. Do not re-implement.

| # | Item | Artifacts |
|---|------|-----------|
| 1–3 | Minimal enrollment + face/voice quality gates | `driveauth/enrollment.py`, `quality_gate.py`, `matchers/face.py`, `/register` |
| 5 | Fleet telemetry pilot (ingest + opt-in reporter) | `POST/GET /api/fleet/telemetry`, `hardware/fleet_telemetry.py` |
| 6 | Bootstrap tuning tooling | `scripts/tune_bootstrap_params.py`, `phases/fleet_bootstrap.env` |
| 7 | CAN retrain orchestration | `scripts/can_retrain_pipeline.py`, `hardware/can_logger.py` |
| 8 | Finger enroll software path | `POST /api/register/finger`, `mean_embed_finger()` |
| 9 | MAP OTP dev docs + mock | `docs/bluetooth-map-setup.md`, `scripts/mock_map_agent.py` |
| 10 | BLE GATT car-side server hook | `DRIVEAUTH_BLE_GATT_ENABLED=1` in dashboard lifespan |
| 11 | TPM config wiring | `DRIVEAUTH_KEY_PROTECTOR=tpm`, `configured_protector()` |
| 12 | Hailo bench harness | `scripts/bench_hailo_face.py` |
| 13 | HIL workflow + runner script | `.github/workflows/hardware-hil.yml`, `scripts/setup_hil_runner.sh` |
| 14 | Compliance engineering checklist | `docs/compliance-checklist.md` |
| 15 | Fairness protocol + proxy script | `docs/fairness-validation-protocol.md`, `scripts/analyze_fairness.py` |
| 16 | Secure-boot rollout plan (doc) | `docs/secure-boot-rollout.md` |
| 17 | Vault policies + ops runbook | `deploy/vault/policies/`, `docs/vault-operations.md`, AppRole in `secrets.py` |
| 4 | Railway deploy runbook + first-boot | `docs/deploy/railway.md`, `scripts/railway_first_boot.sh`, `Dockerfile` |
| 22 | Ladder rigor + mock-finger stage-3 block | `decision_engine.py` / `policy_engine.py`: `min_modalities` / `force_step_up`; mock finger cannot satisfy high-value/bootstrap; missing real stage-3 → `STEP_UP_REQUIRED` |

---

## 👤 YOUR action items (manual — only you can close these)

### Deploy & ops (do first)

- [ ] **(4) Railway `/data` volume** — Follow [`docs/deploy/railway.md`](docs/deploy/railway.md):
  - Create Railway service from this repo
  - Attach volume at `/data`
  - Set env vars (`DRIVEAUTH_DASHBOARD_API_KEY`, `OPENROUTER_API_KEY`, store paths)
  - Retire Cloudflare tunnel demo; use public Railway URL
- [ ] **(5) Point pilot vehicles at collector** — Set on each unit or Railway service:
  ```bash
  DRIVEAUTH_FLEET_TELEMETRY_OPT_IN=1
  DRIVEAUTH_FLEET_TELEMETRY_URL=https://<your-service>/api/fleet/telemetry
  DRIVEAUTH_VEHICLE_ID=vehicle_01
  ```
- [ ] **(6) Tune bootstrap from real data** — After ~1 week of pilot auth:
  ```bash
  python scripts/tune_bootstrap_params.py --store /data/store --write phases/fleet_bootstrap.env
  ```
  Review output, apply env on fleet, commit tuned values.

### Hardware validation (blocked until parts connected)

- [ ] **(7) CAN bus** — Run `driveauth-can-logger` on vehicle; collect `behavioral/*/can_*.csv`; then:
  ```bash
  python scripts/can_retrain_pipeline.py --real-data-dir /var/driveauth/fleet/vehicle_01 --store /data/store
  ```
- [ ] **(8) Fingerprint sensor** — Wire GT-511C3 (3.3 V TTL); `source phases/jetson_orin.env`; `driveauth-finger-daemon`; enroll via `/api/register/finger`; verify live `FingerMatcher` score (not mock). Soft: `DRIVEAUTH_FINGER_SENSOR=gt511`.
- [ ] **(8b) Kinect face/voice** — Xbox 360 Kinect + libfreenect; `DRIVEAUTH_CAMERA_BACKEND=kinect`; `DRIVEAUTH_IR_LIVENESS_ENABLED=1` + `ENSEMBLE=1`; recapture genuines; `driveauth-probe-hw`.
- [ ] **(8c) Jetson Orin first-boot** — `bash scripts/setup_jetson.sh`; confirm CUDA ONNX providers; dashboard + real matchers (no mock).
- [ ] **(9) Bluetooth MAP OTP** — Pair real head unit + phone; follow [`docs/bluetooth-map-setup.md`](docs/bluetooth-map-setup.md); run with `DRIVEAUTH_BT_HW_TEST=1`.
- [ ] **(10) BLE GATT OTP** — Test companion PWA (`companion/ble_otp_pwa/`) against car with `DRIVEAUTH_BLE_GATT_ENABLED=1`; confirm ack roundtrip in logs.
- [ ] **(11) TPM / ATECC** — On TPM-capable board: `DRIVEAUTH_KEY_PROTECTOR=tpm`, enroll driver, reboot, verify templates still load.
- [ ] **(12) Hailo NPU** — On Hailo board: `DRIVEAUTH_HAILO_HW_TEST=1 python scripts/bench_hailo_face.py --store /data/store`.
- [ ] **(13) Self-hosted CI runner** — On a Pi/host with BT + optional Hailo:
  ```bash
  export GITHUB_REPO=<owner>/Drive_auth_edge
  export RUNNER_TOKEN=<from GitHub Actions → Runners → New>
  ./scripts/setup_hil_runner.sh
  ```
  Then trigger **Hardware-in-the-loop** workflow manually.

### Legal & compliance (counsel / field study)

- [ ] **(14) BIPA/GDPR sign-off** — Walk [`docs/compliance-checklist.md`](docs/compliance-checklist.md) with legal counsel; fill sign-off table; do not enroll non-test drivers until done.
- [ ] **(15) Fairness field study** — Run protocol in [`docs/fairness-validation-protocol.md`](docs/fairness-validation-protocol.md); write results to `phases/fairness_report.md`.
- [ ] **(16) Secure-boot on silicon** — Execute phases 1–3 in [`docs/secure-boot-rollout.md`](docs/secure-boot-rollout.md) with OEM/BSP (eFuse, dm-verity, OTA).
- [ ] **(17) Vault in production** — Deploy Vault; apply policies in `deploy/vault/`; provision AppRole per vehicle per [`docs/vault-operations.md`](docs/vault-operations.md); rotate tokens on schedule.

### Publication & launch (deadline-driven)

- [ ] **(18) Paper package** — Fill authors in `docs/paper/*.md`; port to IEEE template; generate Figs 1–3; run claims review vs `docs/security-assumptions.md`; upload PDF/arXiv (`docs/paper/arxiv-metadata.txt`).
- [ ] **(19) Demo video** — Follow shot list in `docs/paper/demo-video.md`; record 5–8 min; publish URL in `phases/phase8.md`.
- [ ] **(20) IEEE IV 2027 submit** — Portal open ~Jul 2026, **deadline 15 Nov 2026**; record submission ID in `phases/phase8.md`.
- [ ] **(21) Social posts** — Publish drafts from `docs/public-posts.md`; paste LinkedIn/Medium URLs back into that file.

---

## Explicit non-goals (do not do yet)

- Do not retrain risk head before ~5k real labeled transactions.
- Do not require high-friction enrollment capture counts as a quality workaround.
- Do not apply `phases/phase2b_suggested.env` as default while face attack/genuine overlap remains high.
- Do not prioritize end-to-end latency optimization before Phase 2a real-model Thor profiling follow-up is complete.
- Do not spend cycles yet on in-graph risk calibration, multimodal home clustering, or SHAP-per-call.

---

## Completed milestones (archived)

- [x] Core Phase 1–7 implementation and benchmarking artifacts
- [x] Own-face migration + Stage-2 PAD/calibrator pipeline
- [x] Standalone OpenRouter + Cloudflare + Railway demo pipeline
- [x] IEEE IV 2027 venue lock + paper/demo draft artifacts
- [x] Minimal-capture enrollment + mandatory face/voice quality gates (items 1–3)
- [x] Biometric ladder enforces fraud/tier modality floors (no voice-only Accept on high-value or bootstrap; mock finger cannot satisfy forced stage-3)
